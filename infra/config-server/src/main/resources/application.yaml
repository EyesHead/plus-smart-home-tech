server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native

  cloud:
    config:
      discovery:
        enabled: true
      server:
        native:
          searchLocations:
            - classpath:config/telemetry/{application}